/**
 * Goal of the course: become a conversant
 * 
 * Versions of JS:
 * - ECMAScript 5 / ES5 -> from 2012
 * - ECMAStript 2015 / ES6
 * - ECMAStript 2016, etc.
 * 
 * Transpilers, use new functionality + keeping compatability:
 * - Babel -> babeljs.io
 * 
 * How to start? ->
 * 1. WebBrowser + JS console -> development tools
 * 2. Node console
*/

function main() {
  let x_1 = "This is a string"; // it is a string literal
  let x_2 = 'This is also a string';
  let x_3 = 'This is "also" a string';
  // console.log(x_1.length);
  // console.log(x_1.toLowerCase());

  // --------------------------------------------------------
  // numbers

  let y_1 = 120; // it is a number
  let y_2 = 120.2399691862937; // it is a float

  // (-)Infinity -> "very large number"
  // NaN -> Not a number, modtly got from an error
  // Math -> module for computations
  // console.log('Rounding', Math.round(y_2));
  // console.log('It is quite random: ', Math.random());
  
  // --------------------------------------------------------
  // Boolean -> true / false
  // === -> is exactly equal?

  // Further reading
  // variables https://javascript.info/variables
  // Data types https://javascript.info/types
}

main()