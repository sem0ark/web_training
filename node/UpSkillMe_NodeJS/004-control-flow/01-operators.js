/**
 * Notes on the course
 * UpSkillMe_Node.js. JavaScript Basics
 * Completed by Arkadii Semenov on 2022.12.12
 * */

(() => {
  // Operator === and !== -> strict equals, the type is checked
  // Operatot == and != -> unstrict equals, some type transitions are applied

  // Arithmetic operators -> + - * /
  // % -> modulus operator
  // ++ / -- -> increment and decrement,
  //      i++ <=> i = i + 1 <=> i += 1
  //      i-- <=> i = i - 1 <=> i -= 1
  // + for trings  == contatenation

  // Logical operators
  // && -> AND
  // || -> OR
  // !  -> NOT

})();
